#inclube <stdio.h>

int main(){}
    int opção, quantidade;
    float total =0, parcial;

    printf("===Lanchonete da Escola===\n");

    do{
        printf("\nCardápio:\n");
        printf("1-Cachoro Quente (R$5,00)\n");
        printf("2-X-Salada (R$8,00)\n");
        printf("3- Refrigerante (R$4,00)\n");
        printf("4- Suco Natural (R$6,00)\n");
        printf("0- Finalizar pedido\n");

        printf("\nEscolha o item:");
        scanf("%d",&opção);

        if (opção==0)break;

        printf("Informe a quantidade:");
        scanf("%d",&quantidade);
        switch(opção) {
            case 1:
            parcial= quantidade *5.0;
            printf("Você pediu %d Cachorros(s) Quente: R$%.2f\n", quantidade, parcial);
            total += parcial;
            break;
            case 2:
            parcial = quantidade *8.0;
            printf("Você pediu %d X-salada(s):R$5.2f\n", quantidade, parcial);
            total +=parcial;
            break;
            case 3:
            parcial = quantidade *4.0;
            printf("Você pediu %d Refrigerante(s):R$%.2f\n",quantidade, parcial)
            total += parcial:
            break;
            case 4:
            parcial = quantidade *6.0;
            printf("Vocẽ pediu %d Suco(s) Natural: R$%.2f\n");
            total += parcial
            break;
            default:
            printf("Opção inválida! Tente novamente.\n");
        }

    } while (opcao!=0);

    printf("\n=== TOTAL A PAGAR: R$%.2f===\n", total);
    printf("Obrigado pela preferência");
